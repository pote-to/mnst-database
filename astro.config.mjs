import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // site: 'https://pote-to.github.io',
    // site: 'https://pote-to.github.io/mnst-database',
    // base: '/mnst-database',
});
